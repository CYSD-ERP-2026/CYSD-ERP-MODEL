# CYSD ERP – Environment Configuration
# Copy this file to .env and fill in the values before running the project.
# Never commit .env to version control.

# Django
SECRET_KEY=django-insecure-replace-with-a-long-random-string-in-production
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# PostgreSQL
DB_NAME=cysd_erp_db
DB_USER=postgres
DB_PASSWORD=RASAYAMdata@2026
DB_HOST=localhost
DB_PORT=5432
